{
  "name": "acamus_typescript",
  "version": "1.0.0",
  "description": "Acamus Playwright TypeScript - POM Framework",
  "scripts": {
    "test": "npx playwright test",
    "test:login": "npx playwright test tests/login.test.ts",
    "test:headed": "npx playwright test --headed",
    "test:report": "npx playwright show-report reports/html",
    "lint": "npx tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
